# GitHub

Workflow predisposti:

- `quality.yml`: validazione su push e pull request.
- `deploy-staging.yml`: automatico su push dei branch abilitati, manuale con dry-run predefinito, solo staging.
- `verify-siteground.yml`: manuale e read-only.
