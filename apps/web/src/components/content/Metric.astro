---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="metric" data-reveal="scale">
  <strong data-motion-count={value}>{value}</strong>
  <p>{label}</p>
</div>
