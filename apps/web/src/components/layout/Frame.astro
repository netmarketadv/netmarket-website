---
interface Props {
  class?: string;
  radius?: string;
}

const { class: className = '', radius } = Astro.props;
---

<div class:list={['nm-frame', className]} style={radius ? `--frame-radius: ${radius};` : undefined}>
  <slot />
</div>
