<script>
  import { initMotion } from '@/lib/motion/lifecycle';

  const fullMotion = document.documentElement.dataset.motionMode !== 'light';
  initMotion({
    useGsap: fullMotion,
    useGsapForLines: !fullMotion
  });
</script>
